[project]
name = "interview-practice"
version = "0.1.0"
description = "Interview prep scripts and practice problems"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "polars",
    "pyarrow>=23.0.1",
    "requests",
    "scipy",
]
